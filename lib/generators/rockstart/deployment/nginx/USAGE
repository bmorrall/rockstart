Description:
    Configures a Rails app to work with NGINX

Example:
    rails generate rockstart:deployment:nginx

    This will create:
        Configure sendfile parameters in environments/production.rb
